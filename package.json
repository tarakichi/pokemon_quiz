{
    "name": "pokemon-quiz-server",
    "version": "1.0.0",
    "main": "server/server.js",
    "scripts": {
        "start": "node server/server.js",
        "dev": "concurrently \"cd client && npm run dev\" \"npx ts-node server/index.ts\""
    },
    "devDependencies": {
        "concurrently": "^9.2.0",
        "ts-node": "^10.9.2"
    }
}
